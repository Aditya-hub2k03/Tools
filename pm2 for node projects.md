command to install: npm install -g pm2

command to start: pm2 start npm --name "my-next-app" -- run start

PM2 saves logs to specific locations (e.g., ~/.pm2/logs/) and provides commands to view them, such as pm2 logs my-next-app
